services:
  backend:
    container_name: dsa-search-engine-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    restart: always
